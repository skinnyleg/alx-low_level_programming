#include "main.h"

/**
 * swap_int - swap the values of
 * a and b
 *
 * @a: pointer to an int variable
 * @b: pointer to an int variable
 */

int _strlen(char *s)
{
	int i = 0;
	while (s[i] != '\0')
		i++;
	return (i);
}
